import React, { Component } from "react";

export default class Navbar extends Component {
    render() {
        return [
            <h3 key={1} className="mt-2 text-center">
                MQTT Chat
            </h3>
        ]
    }
}
